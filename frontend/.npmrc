# package.json 釘了 packageManager: pnpm@11.10.0,pnpm 預設會自己去下載那個版本
# 再切過去;下載失敗(網路慢、公司網路擋、沒有寫入權限)就整個建置中止，
# 即使機器上已經有能用的 pnpm 也不給用 —— SteamCMD 版的一鍵安裝就是卡在這裡。
#
# 這個設定關掉自動切換：用使用者現有的 pnpm 建置即可（lockfile 相容 pnpm 10/11，
# 實測 10.16.1 也能正常 install + build）。寫在 .npmrc 而不是環境變數，
# 是因為 build 腳本內部還會再開子 pnpm，環境變數不一定傳得下去。
manage-package-manager-versions=false
